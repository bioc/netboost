% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{cpp_mcupgma}
\alias{cpp_mcupgma}
\title{In-memory MC-UPGMA clustering (single round)}
\usage{
cpp_mcupgma(low, high, dist, max_singleton, max_distance)
}
\arguments{
\item{low}{Integer vector, smaller cluster id of each input edge (1-based).}

\item{high}{Integer vector, larger cluster id of each input edge (1-based).}

\item{dist}{Numeric vector of edge distances (in [0, max_distance]).}

\item{max_singleton}{Numeric. Maximum singleton id; new (merged) cluster ids
start at \code{max_singleton + 1}.}

\item{max_distance}{Numeric. Upper distance bound (psi); also the distance
assigned to missing pairs.}
}
\value{
Numeric matrix with columns cluster_id1, cluster_id2, distance,
  cluster_id3 (one row per merge, in merge order).
}
\description{
Portable reimplementation of the sparse average-linkage
  (UPGMA) clustering performed by netboost. Missing pairs are treated as
  distance \code{max_distance}; merging proceeds in order of increasing
  distance (ties broken by the largest cluster-id pair) until no edge with
  distance <= \code{max_distance} remains, yielding a forest.
}
